const status=document.getElementById('map-status');const fallback=document.getElementById('map-fallback');const query='Transamerica SP, São Paulo, Brasil';const map=L.map('map').setView([-23.5505,-46.6333],11);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'&copy; OpenStreetMap contributors'}).addTo(map);fetch('https://nominatim.openstreetmap.org/search?format=json&limit=1&q='+encodeURIComponent(query)).then(r=>r.json()).then(items=>{if(items.length){const p=items[0];map.setView([+p.lat,+p.lon],16);L.marker([+p.lat,+p.lon]).addTo(map);status.textContent='Local encontrado no OpenStreetMap.';}else throw 0;}).catch(()=>{status.textContent='Falha ao localizar automaticamente.';fallback.hidden=false;});