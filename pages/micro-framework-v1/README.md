# Pages Micro Framework v1

Build-free micro-framework for event landing pages under `pages/`.

## Structure

- `core/`: shared tokens, base layout and runtime helpers.
- `components/`: reusable page components.
- `templates/`: copyable templates for new pages.

No build step is required. Pages import CSS/JS with relative paths.
